

一、接口：

1、napi_get_reference_value

参数说明： 
           1. `env`：Node.js 的环境对象。
           2. `ref`：要获取值的 JavaScript 对象引用的句柄。
           3. `result`：指向获取到的 JavaScript 对象值的指针。
使用场景：

5、napi_create_object

2、napi_create_reference

2、napi_call_function

3、napi_call_function

4、napi_wrap

5、napi_get_cb_info

7、napi_get_value_string_utf8

8、napi_typeof

9、napi_close_handle_scope

10、napi_delete_reference