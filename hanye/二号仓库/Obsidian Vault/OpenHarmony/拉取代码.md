**OpenHarmony主干代码获取 方式二：通过repo + https下载。**

```
repo init -u <https://gitee.com/openharmony/manifest.git> -b master --no-repo-verify
```
```
repo sync -c
```
```
repo forall -c 'git lfs pull'
```
```
bash build/prebuilts_download.sh
```



``