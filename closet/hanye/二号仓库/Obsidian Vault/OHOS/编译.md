SDK

```Bash
./build.sh --product-name ohos-sdk --target-cpu arm64
```
Image
```Bash
./build.sh --product-name rk3568
```

SDK

```Bash
./build.sh --product-name ohos-sdk --target-cpu arm64 --ccache --fast-rebuild
```
Image
```Bash
./build.sh --product-name rk3568 --ccache --fast-rebuild
```