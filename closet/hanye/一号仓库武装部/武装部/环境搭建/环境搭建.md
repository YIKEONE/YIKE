
*******************************************************************
替换镜像

sudo cp /etc/apt/sources.list /etc/apt/sources.list.bak
sudo gedit /etc/apt/sources.list

# ubuntu版本查看命令 lsb_release -a
# 18.04
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ bionic main restricted universe multiverse
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ bionic-updates main restricted universe multiverse
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ bionic-backports main restricted universe multiverse
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ bionic-security main restricted universe multiverse

# 20.04
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal main restricted universe multiverse
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal-updates main restricted universe multiverse
deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ focal-backports main restricted universe multiverse
deb http://security.ubuntu.com/ubuntu/ focal-security main restricted universe multiverse

# 清华镜像源官网：https://mirrors.tuna.tsinghua.edu.cn/help/ubuntu/
验证 apt 工作是否正常

sudo apt-get update

sudo apt-get upgrade


*******************************************************************
# ubuntu侧

SSH配置（方便和window通讯）
安装
（1）SSH安置服务 
sudo apt install openssh-server
（2）启动SSH服务：
sudo systemctl start ssh
（3）使SSH服务开机自启：
sudo systemctl enable ssh
（4）配置防火墙允许SSH连接（默认端口22）：
sudo ufw allow ssh
*****************************************************************
window侧安装
1.安装MobaXterm

*****************************************************************

Ubuntu侧安装

1.下载工具
sudo apt-get install binutils binutils-dev git git-lfs gnupg flex bison gperf build-essential zip curl zlib1g-dev libc6-dev-i386 lib32ncurses5-dev x11proto-core-dev libx11-dev lib32z1-dev ccache libgl1-mesa-dev libxml2-utils xsltproc unzip m4 bc gnutls-bin python3.8 python3-pip ruby genext2fs device-tree-compiler make libffi-dev e2fsprogs pkg-config perl openssl libssl-dev libelf-dev libdwarf-dev u-boot-tools mtd-utils cpio doxygen liblz4-tool openjdk-8-jre gcc g++ texinfo dosfstools mtools default-jre default-jdk libncurses5 apt-utils wget scons python3.8-distutils tar rsync git-core libxml2-dev lib32z-dev grsync xxd libglib2.0-dev libpixman-1-dev kmod jfsutils reiserfsprogs xfsprogs squashfs-tools pcmciautils quota ppp libtinfo-dev libtinfo5 libncurses5-dev libncursesw5 libstdc++6 gcc-arm-none-eabi vim ssh locales libxinerama-dev libxcursor-dev libxrandr-dev libxi-dev



2.将Ubuntu Shell环境修改为bash
(1)执行如下命令，确认输出结果为bash。如果输出结果不是bash，
ls -l /bin/sh
请根据子步骤(2)，将Ubuntu shell修改为bash。
(2)打开终端工具，执行如下命令，输入密码，然后选择No，将Ubuntu shell由dash修改为bash。
sudo dpkg-reconfigure dash


3.将Python 3.8设置为默认Python版本
(1)查看Python 3.8的位置：
which python3.8
(2) 将Python和Python3切换为Python 3.8：
sudo update-alternatives --install /usr/bin/python python {Python 3.8 路径} 1
 #{Python 3.8 路径}为上一步查看的Python 3.8的位置
sudo update-alternatives --install /usr/bin/python3 python3 {Python 3.8 路径} 1
sudo update-alternatives --install /usr/bin/python3 python3 /usr/bin/python3.8 1
 #{Python 3.8 路径}为上一步查看的Python 3.8的位置


4.配置用户信息
git config --global user.name " yangziyon"
git config --global user.email " nsyangziyong@huawei.com"
git config --global credential.helper store


5.Copy ssh秘钥到.ssh目录
将yangzy/.ssh目录拷贝到~/.ssh

chmod 600 id_rsa
chmod 600 id_rsa.pub

6.安装repo

#mkdir ~/bin
#curl https://mirrors.tuna.tsinghua.edu.cn/git/git-repo -o ~/bin/repo 
#chmod a+x ~/bin/repo
#pip3 install -i https://repo.huaweicloud.com/repository/pypi/simple requests

#https://gitee.com/oschina/repo/raw/fork_flow/repo-py3

#git clone https://mirrors.tuna.tsinghua.edu.cn/git/git-repo


*********************************************************************
git clone https://mirrors.tuna.tsinghua.edu.cn/git/git-repo
mkdir ~/.bin
cp git-repo/repo ~/.bin
sudo chmod a+x ~/.bin/repo
export PATH=~/.bin:$PATH
echo "export PATH=~/.bin:$PATH">>~/.bashrc

export REPO_URL='https://mirrors.tuna.tsinghua.edu.cn/git/git-repo/'
echo "export REPO_URL='https://mirrors.tuna.tsinghua.edu.cn/git/git-repo/'">>~/.bashrc

**********************************************************************

sudo mkdir ~/bin  
cd  ~/bin  #进入目录
sudo curl https://mirrors.tuna.tsinghua.edu.cn/git/git-repo -o repo   #下载REPO
sudo chmod a+x ~/bin/repo   #修改权限
export PATH=~/bin:$PATH #添加变量
source ~/.bashrc    #更新环境变量
repo --version #查看版本
****************************************************************************
7.获取主干代码
mkdir work
cd work
mkdir code
cd code
repo init -u git@gitee.com:openharmony/manifest.git -b master --no-repo-verify
repo init -u https://gitee.com:openharmony/manifest.git -b master --no-repo-verify
repo init -u OpenHarmony/manifest -b master --no-repo-verify

repo sync -c

repo forall -c 'git lfs pull'

8.执行Prebuild
bash build/prebuilts_download.sh

9.编译源码
./build.sh --product-name rk3568 --ccache

10.快速差分编译：
./build.sh --product-name rk3568 --gn-args enable_notice_collection=false --gn-args load_test_config=false --gn-args enable_lto_O0=true --fast-rebuild --ccache
并行下载代码并强行合并repo：
repo sync -c -j16 --force-sync

******************************************************************************************
编译环境需要Ubuntu18.04及以上版本。

安装编译所需的程序包。

安装命令：
sudo apt-get install bison ccache default-jdk flex gcc-arm-linux-gnueabi gcc-arm-none-eabi  genext2fs liblz4-tool libssl-dev libtinfo5 mtd-utils mtools openssl ruby scons unzip u-boot-tools  zip

```
./build.sh --product-name {product_name}
```

编译结果镜像输出在 out/${device_name}/packages/phone/images/目录下。

**************************************************************************************************************
ohos-sdk编译：

**编译命令：**

``` ./build.sh --product-name ohos-sdk --ccache ```------在代码位置执行此命令即可

**结果输出：**

``` out/sdk/packages/ohos-sdk/ ```

**ohos-sdk模块配置：**

``` build/ohos/sdk/ohos_sdk_description_std.json ```

SDK编译命令：./build.sh --product-name ohos-sdk --ccache

SDK路径：out/sdk/packages/ohos-sdk/

Deveco Studio SDK配置方法：Ubuntu编译SDK，替换到Windows环境Studio的SDK

注意事项：* 文件夹，命名为11
                *  修改oh-uni-package.json问，12->11

******************************************************************************************************************

DevEco-4.1_Preview配置
  1. 准备好v18.0.0以上的Nodejs
  2. 准备ohpm-1.3.0
 3. 将以下内容添加/更新到C:\Users\{yourname} \.npmrc文件。如果没有.npmrc文件，则需要新创建一个
registry=https://repo.huaweicloud.com/repository/npm/
@ohos:registry=https://repo.harmonyos.com/npm/

 4. 将以下内容添加/更新到C:\Users\{yourname} \.ohpm\.ohpmrc。如果没有文件文件夹，则需要新创建一个
registry=https://repo.harmonyos.com/ohpm/
strict_ssl=false

2. 安装DevEco4.1 Canary版本
下载DevEco4.1 Canary，并安装，启动。
3.资料及工程创建按照
资料：https://neusoft.feishu.cn/docx/KDULdiC1CoAF6kxpGsGcXcNbnch
 注：最后出现问题需观看main中代码是否正确

*********************************************************************************************************************


